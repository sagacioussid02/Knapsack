package algorithms.concepts;

/**
 * Given a set of items where each item has a weight and a value. And given a
 * knapsack with max weight capacity, determine the maximum value that can be
 * placed into the knapsack without going over the capacity.
 * 
 * @author SS057631
 *
 */
public class Knapsack {
	
	/**
	 * Example :
	 * Input:  weights = [10, 20, 30]
	 *  values =  [60, 100, 120]
	 *  capacity = 50
	 *  Output: 220
	 * @param args
	 */
	
	public int knapsack(int[] weights, int[] values, int capacity) {
		//tabulation
		//get the maximum values possible for each weight till the capacity
		
		//iterate through all the available weights
		
		//iterate through all the possible weights till the capacity
		  //get the maximum value possible 
		return 0;
	}

	public static void main(String[] args) {
		

	}

}
